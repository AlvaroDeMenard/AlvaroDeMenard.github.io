!function($){"use strict";function CodeBlockResizer(elem){this.$codeBlocks=$(elem)}CodeBlockResizer.prototype={run:function(){var self=this;self.resize(),$(window).smartresize(function(){self.resize()})},resize:function(){this.$codeBlocks.each(function(){var $gutter=$(this).find(".gutter"),$code=$(this).find(".code"),codePaddings=$code.width()-$code.innerWidth(),width=$(this).outerWidth()-$gutter.outerWidth()+codePaddings;$code.css("width",width),$code.children("pre").css("width",width)})}},$(document).ready(function(){$.fn.hasHorizontalScrollBar=function(){return this.get(0).scrollWidth>this.innerWidth()},new CodeBlockResizer("figure.highlight").run()})}(jQuery);